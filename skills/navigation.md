# Navigation: skills/

## What lives here?

All active skills. Each skill is a sub-directory with `SKILL.md` as
SoT. Optional further files: `REFERENCE.md` for off-loaded detail,
prompts / templates / examples — cross-reference rather than
duplicate.

**Skill format:** v2 is mandatory. Frontmatter with `description`
(use-when), `invocation.primary`. Format
standard: `framework/skill-anatomy.md`. Validator:
`scripts/skill_fm_validate.py` (pre-commit check 7, BLOCK).

**Classification:** single-class model. All skills are equal.
Workflow is a separate layer (`workflows/runbooks/`); protocols are
a separate asset type (`_protocols/`).

**`_protocols/`:** shared loads, referenced by other skills, never
called directly. Its own navigation lives in
`_protocols/navigation.md`.

## Where to look for which question?

| Question | Skill |
|---|---|
| "Problem unclear, solution space open" | `frame/` |
| "Write a spec for a new feature" | `spec_authoring/` |
| "Review an existing spec (rebuild-ready?)" | `spec_board/` |
| "Update a spec against the as-is code" | `retroactive_spec_update/` |
| "Review a code diff (L1/L2)" | `code_review_board/` |
| "Architecture decision with multiple perspectives" | `council/` |
| "Check an implementation plan before coding" | `impl_plan_review/` |
| "Fix a bug with root-cause analysis" | `root_cause_fix/` |
| "Design / run tests" | `testing/` |
| "Design APIs / interfaces (Hyrum, versioning)" | `api_and_interface_design/` |
| "Plan a production deploy (pre-launch, rollback)" | `shipping_and_launch/` |
| "Remove an old system (strangler, churn)" | `deprecation_and_migration/` |
| "Improve codebase architecture (Pocock lift)" | `improve_codebase_architecture/` |
| "Security methodology (OWASP, auth, input validation, secrets)" | `security_and_hardening/` |
| "Build frontend / UI (a11y, design system, states)" | `frontend_ui_engineering/` |
| "Cross-spec coherence review" | `architecture_coherence_review/` |
| "Verify a spec amendment" | `spec_amendment_verification/` |
| "Sectional deep-review of a large spec" | `sectional_deep_review/` |
| "Bounded fix → review iteration" | `convergence_loop/` |
| "Repo integrity check (dead refs, orphans, drift)" | `consistency_check/` |
| "Write ADR / docs / changelog" | `documentation_and_adrs/` |
| "Persist knowledge from a conversation" | `knowledge_capture/` |
| "Write into the context system" | `knowledge_processor/` |
| "First-principles drill" | `bedrock_drill/` |
| "Fetch external API docs" | `get_api_docs/` |
| "Create a task / change status" | `task_creation/` + `task_status_update/` |
| "Token-compression mode" | `caveman/` |
| "Unknown code area: give me the map" | `zoom_out/` |
| "Pre-build spec audit / MVP reconciliation" | `pre_build_spec_audit/` |
| "Headers on delegation / return summary?" | `transparency_header/` + `return_summary/` |
| "Python conventions, Ruff / Mypy" | `python_code_quality_enforcement/` |
| "Process a video via subtitles" | `youtube_subtitles/` |

## Inventory (auto)

<!-- NAV-AUTO-START -->

_Generated by `scripts/generate_navigation.py` — do not edit_

### Active Skills (Sub-Directories)

| Sub-dir | Description |
|---|---|
| `adversary_test_plan/` | Adversary-driven test-plan extension BEFORE implementation. Code-adversary reviews the tester design output and adds edge-case TCs the implementer's cognitive bias systematically misses. RED tests… |
| `api_and_interface_design/` | API and interface design discipline. Hyrum's law + one-version rule + contract-first + validate-at-boundaries + prefer-addition-over-modification + predictable-naming. Use when designing new APIs,… |
| `architecture_coherence_review/` | Cross-spec coherence review for specs that share contracts. 7-dimension extraction, interface-contract assembly, advisory board. Formalized from Task 274 experience. |
| `bedrock_drill/` | Recursive first-principles drill with bottom-up synthesis. Decomposes problems down to axiom level (physics / logic / values), then builds answers from the bottom up. Domain-independent. |
| `caveman/` | Ultra-compressed communication mode. ~75% token reduction by dropping filler / articles / pleasantries while keeping full technical accuracy. Use when the user says "caveman mode", "talk like… |
| `code_review_board/` | Multi-perspective code review. 2 levels: L1 (focused) + L2 (full board). |
| `consistency_check/` | Checks the structural integrity of the repo. Dead references, orphan files, adapter SoT drift, navigation desync. |
| `convergence_loop/` | Intra-gate iteration protocol. Bounded convergence with a rising severity threshold and narrowing scope. Max 3 passes. |
| `council/` | Structured decision-making. Two modes: (a) Architectural — 3-4 council-member subagents in parallel, context isolation, Buddy consolidates. (b) Interactive — Buddy moderates a user dialog with… |
| `cross_spec_consistency_check/` | Pre-authoring conflict detection between specs in the source-grounding set of the current build run. Four pattern classes (API naming drift, concept double authority, schema drift, authority… |
| `deprecation_and_migration/` | Deprecation and migration discipline. Code-as-liability mantra + compulsory-vs-advisory decision + strangler / adapter / feature-flag patterns + churn rule + zombie-code handling. Use when removing… |
| `documentation_and_adrs/` | Capture decisions and documentation — why, alternatives, trade-offs. ADR discipline, README / changelog, API docs, agent-ready rules (CLAUDE.md). Adapted from addyosmani/agent-skills; MIT, see ##… |
| `frame/` | Board-style problem-analysis discipline. Forces multi-perspective thinking before solutions are developed. Output: a structured frame report with reformulated problem, solution space, and… |
| `frontend_design_tty/` | Design system for dark, terminal-inspired web UIs (monospace, mint/sage on dark blue-grey, box drawing, hex labels, no rounded corners). Provides concrete CSS custom properties + component patterns… |
| `frontend_ui_engineering/` | Production-quality UI discipline. Component architecture, state decisions, design-system adherence (no AI aesthetic), a11y (WCAG 2.1 AA), responsive, meaningful empty/loading/error states. Code-side… |
| `get_api_docs/` | Fetch current API documentation for external libraries before coding against them. Prevents hallucinated parameters and stale interfaces. |
| `impl_plan_review/` | Multi-perspective review of the implementation plan BEFORE coding. Closes the code-synthesis gap: MCA's interpretation of the spec is validated before code is written. |
| `improve_codebase_architecture/` | Codebase-wide architecture improvement via the deep-modules pattern (Ousterhout / Feathers). Strict glossary (module / interface / implementation / depth / seam / adapter / leverage / locality).… |
| `knowledge_capture/` | Persist knowledge from context windows, conversations, and research sessions. Larger knowledge artifacts (research, SOTA, solve states, interview insights) — not single facts (that's… |
| `knowledge_processor/` | Brain logic as agent behaviour. Process new information against existing knowledge. Keeps `context/` accurate. Pre-brain: LLM on files; post-brain: queries. |
| `pre_build_spec_audit/` | Concept mining + MVP reconciliation against pre-existing specs. Looks at pre-MVP / pre-build specs for methodology, architecture ideas, and patterns — compared in the current MVP scope, not as a… |
| `python_code_quality_enforcement/` | Python code quality and conventions for BuddyAI. Tooling, layouts, coding standards, and security checks for all Python code. |
| `retroactive_spec_update/` | Retroactively update existing specs to match the as-is code state. Git commits give SCOPE, source code is the EVIDENCE. Walk through the spec section by section, read the relevant source files… |
| `return_summary/` | Format and rules for sub-agent return summaries. Load before step 8 (return). |
| `risk_followup_routing/` | Routes chief-verdict `remaining_findings:` entries per their `target:` annotation (6-value enum: spec_text / new_task / watch_item / absorb_next / closes_with / re_review). Replaces the legacy… |
| `root_cause_fix/` | 5-step root-cause analysis and fix. Structured fix lifecycle for every defect, no matter how small. |
| `scoping/` | Translate a high-level intent into an approved spec hierarchy (L0-L3). Buddy guides the user progressively through every level. |
| `sectional_deep_review/` | Deep review for large foundation specs (>1000 lines, many subsections). Splits the spec into pattern groups, reviews each with cross-reference context, fixes per group, then runs a full-spec review… |
| `security_and_hardening/` | Security-first development discipline. Treat external input as hostile, secrets as sacrosanct, authorization checks as required. Methodology layer for the security / code-security agent personas. |
| `shipping_and_launch/` | Production-launch discipline with pre-launch checklist + feature-flag strategy + staged rollout + rollback plan + monitoring. Use when deploying to production, preparing a release, migrating data /… |
| `source_spec_reduce/` | Post-authoring source-spec reduction: cut each absorbed section + leave a pointer stub. Required output: drift-items.yaml with 3-way triage (existing-task / new-task / die). When the source is fully… |
| `spec_amendment_verification/` | Read-only cross-spec verification after spec amendments. Checks whether the changes are consistent with the referenced specs. |
| `spec_authoring/` | Interview-based authoring of a NEW spec. Solution-space exploration, artifact checklist, intent_chain + intent alignment validation. For updating existing specs: spec_update skill. For review:… |
| `spec_board/` | Multi-perspective spec quality review. Checks whether an existing spec is rebuild-ready: can an implementer rebuild it 1:1 from the spec alone? 5 dimensions: completeness, consistency,… |
| `spec_update/` | DEPRECATED (2026-04-10). Split into two more specific skills. Use retroactive_spec_update for code-as-evidence catch-up (existing specs drifted from code). Use spec_authoring for new specs or new… |
| `task_creation/` | Structured task creation. Self-contained tasks with ACs, intent_chain, and a duplicate check. Task quality determines downstream quality. |
| `task_status_update/` | Atomic task-status change. The only allowed way to change `status` in task YAMLs. Writes YAML + backlog in one operation. |
| `testing/` | Methodology and formats for test-case design and execution across the entire SSDLC. Code tests, logic, processes, specs, constraints. Domain-specific eval patterns: testing/eval_patterns/. |
| `transparency_header/` | Header formats for delegation, execution, and direct conversation. Print on every delegation and task start. No opt-in. |
| `youtube_subtitles/` | Read YouTube videos via subtitles instead of watching them. For research, references, and knowledge extraction. |
| `zoom_out/` | User-triggered: provide a wider context / higher abstraction level for an unfamiliar code area. Use when the user is unfamiliar with a section, asks "how does X fit into the picture", or mentions… |

<!-- NAV-AUTO-END -->
